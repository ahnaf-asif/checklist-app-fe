import { Box } from '@mantine/core';
import { Layout } from '@/Layouts';

export const Home = () => {
  return (
    <Layout showSidebar>
      <Box>
        <Box>This is home</Box>
      </Box>
    </Layout>
  );
};
