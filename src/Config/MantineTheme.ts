import { MantineThemeOverride } from '@mantine/core';

const customThemeConfig: MantineThemeOverride = {
  fontFamily: 'inter ,sans-serif',
  defaultRadius: '3px'
};

export default customThemeConfig;
